meta:
  environment: python
  toolchain:
    name: pip
    version: "3.14"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  scriptName: manage.py
  command: runserver
  persistenceMount: /data
  containerPort: "80"
